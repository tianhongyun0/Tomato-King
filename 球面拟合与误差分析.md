# 球面拟合与误差分析

本文档与工程脚本 `sphere_fitting.py` 中的实现一致。下面公式使用 **LaTeX**：行内用 `$...$`，重要式子用单独一行的 `$$...$$`（在 VS Code、Typora、GitHub 等支持 Math 的预览中可直接渲染）。若只看源码，也可直接读 `$$` 里的式子。

表述上区分：**随机误差**、**系统误差**、**不可辨识性**（见第 1 节）。

---

## 1、误差来源分析（补充）

### 1.1 全站仪观测链路与随机误差

**观测量**：第 $i$ 个点有水平方向角 $\mathrm{Hz}_i$、竖直角 $V_i$、斜距 $d_i$（脚本里由度分秒换算为弧度后参与计算）。

**单位方向**（与 `measurement_to_unit_vector` 一致）：

$$
\mathbf{u}_i=
\begin{bmatrix}
\cos V_i \sin \mathrm{Hz}_i \\
\cos V_i \cos \mathrm{Hz}_i \\
\sin V_i
\end{bmatrix},
\qquad \|\mathbf{u}_i\|=1.
$$

**笛卡尔坐标**（单位：脚本中为 mm，即 $d_i$ 以 mm 代入）：

$$
\mathbf{P}_i = d_i\,\mathbf{u}_i
=
\begin{bmatrix}
X_i \\ Y_i \\ Z_i
\end{bmatrix}
=
d_i
\begin{bmatrix}
\cos V_i \sin \mathrm{Hz}_i \\
\cos V_i \cos \mathrm{Hz}_i \\
\sin V_i
\end{bmatrix}.
$$

**一阶误差传播（协方差）**：设水平角、竖直角、斜距的方差分别为 $\sigma_{\mathrm{Hz}}^2,\sigma_V^2,\sigma_d^2$（脚本中对应 `--sigma-angle-arcsec`、可选 `--sigma-v-arcsec`、`--sigma-distance-mm`），且三者**互不相关**，则

$$
\boldsymbol{\Sigma}_i
=
\mathbf{J}_i
\begin{bmatrix}
\sigma_{\mathrm{Hz}}^2 & 0 & 0 \\
0 & \sigma_V^2 & 0 \\
0 & 0 & \sigma_d^2
\end{bmatrix}
\mathbf{J}_i^{\mathsf{T}},
$$

其中 $\mathbf{J}_i$ 为三列，依次为 $\mathbf{P}$ 对 $\mathrm{Hz}$、对 $V$、对 $d$ 的偏导（见代码 `point_covariance_from_total_station`）：

$$
\mathbf{J}_i =
\begin{bmatrix}
\dfrac{\partial X_i}{\partial \mathrm{Hz}_i} &
\dfrac{\partial X_i}{\partial V_i} &
\dfrac{\partial X_i}{\partial d_i} \\[6pt]
\dfrac{\partial Y_i}{\partial \mathrm{Hz}_i} &
\dfrac{\partial Y_i}{\partial V_i} &
\dfrac{\partial Y_i}{\partial d_i} \\[6pt]
\dfrac{\partial Z_i}{\partial \mathrm{Hz}_i} &
\dfrac{\partial Z_i}{\partial V_i} &
\dfrac{\partial Z_i}{\partial d_i}
\end{bmatrix}.
$$

例如第三列（对斜距）由 $\mathbf{P}_i=d_i\mathbf{u}_i$ 直接得到：

$$
\frac{\partial \mathbf{P}_i}{\partial d_i} = \mathbf{u}_i.
$$

**直观结论**：同样角秒噪声下，$d_i$ 越大，由角度引起的**横向**坐标扰动越大；$\boldsymbol{\Sigma}_i$ 一般**不是**单位阵的倍数，因此“各点等权、各向同性”的假设常常不成立。

**严谨表述**：$\boldsymbol{\Sigma}_i$ 是**一阶近似**；角度误差很大时二阶项不可忽略（工程角秒 + 米级距离通常仍可接受）。

### 1.2 几何构形（可观测性）与参数不确定度

待求参数：球心 $\mathbf{C}=(C_x,C_y,C_z)^{\mathsf{T}}$，半径 $R$，共 4 个未知数。

**几何残差**（第 2 节会反复出现）：

$$
r^{\mathrm{geo}}_i(\mathbf{C},R) = \|\mathbf{P}_i-\mathbf{C}\|-R.
$$

若点只覆盖球面一块区域，某些方向上 $\mathbf{C}$ 与 $R$ **强耦合**，法向量张成近似低秩 → 正规方程病态 → **参数方差大**；但这与 **RMS 是否大**不是同一回事。

### 1.3 模型误差（系统项）

若真实目标不是理想球、或存在未建模偏心/改正项，则不存在一组 $(\mathbf{C},R)$ 使所有 $r^{\mathrm{geo}}_i$ 同时接近 0；此时 RMS 有**模型误差底**，加权或 LM **不能**代替改模型。

### 1.4 粗差与稳健性

粗差会拉偏普通最小二乘；脚本主线未默认启用稳健 M 估计 / RANSAC。

### 1.5 算法层数值误差（通常次要）

例如用 $\ell=\ln R$ 参数化、或对 $\mathbf{J}^{\mathsf{T}}\mathbf{J}$ 加阻尼，主要为**数值稳定与避免 $R\le 0$**，不改变“测什么、建什么模”的本质。

---

## 2、优化尝试（`sphere_fitting.py` 各方法说明）

### 2.0 阅读说明

- 输入统一得到 $\mathbf{P}_i$；除 A/B 类外还计算 $\boldsymbol{\Sigma}_i$。
- 下面按**数学目标**分 A～E 五类；**同类内**多个 `--method` 往往只求解器不同，$(\mathbf{C},R)$ 会非常接近。

```
                    ┌─ A 线性代数初值 ─── algebraic
                    │
                    ├─ B 无加权几何距离 ─ geometric-ls / geometric-lm / gauss-newton-geometric
                    │
  Hz,V,d → P_i, Σ_i ─┼─ C 协方差加权（法向标量）─ weighted-geometric-ls / -lm / marquardt / sequential-ekf
                    │
                    ├─ D 协方差 + 近似马氏垂足（3 残差/点）─ mahal-orthogonal-ls
                    │
                    └─ E 观测域射线+斜距 ─ directional-ls
```

---

### 2.1 A 类：线性代数初值 — `algebraic`

**干什么**：一次线性最小二乘求近似球参，**不作**最终几何距离最优解；常作非线性迭代的初值。

---

**第 1 步：球面隐式方程（对球面上任意一点 $(x,y,z)$）**

$$
(x-C_x)^2+(y-C_y)^2+(z-C_z)^2=R^2.
$$

---

**第 2 步：把左端三个平方全部乘开（逐项展开）**

$$
\bigl(x^2-2C_x x+C_x^2\bigr)
+\bigl(y^2-2C_y y+C_y^2\bigr)
+\bigl(z^2-2C_z z+C_z^2\bigr)
=R^2.
$$

去掉括号、只把字母按项排开，等价于：

$$
x^2+y^2+z^2
-2C_x x-2C_y y-2C_z z
+C_x^2+C_y^2+C_z^2
=R^2.
$$

---

**第 3 步：整理成「未知数在左、只含测点坐标在右」**

把上式中含 $(C_x,C_y,C_z,R)$ 但与 $(x,y,z)$ **无关**的常数项 $R^2-C_x^2-C_y^2-C_z^2$ 移到等号一边，把只含测点 $(x,y,z)$ 的项移到另一边。两边同时加减整理后，得到与下面**同一条**一次关系等价的形式。

对第 $i$ 个测量点，坐标为 $(x_i,y_i,z_i)$，上式写成：

$$
2C_x x_i + 2C_y y_i + 2C_z z_i
\;+\;
\bigl(R^2-C_x^2-C_y^2-C_z^2\bigr)
=
x_i^2+y_i^2+z_i^2.
$$

---

**第 4 步：引入记号 $b$（把括号里那一整块球参单独起一个名字）**

上面括号里**只与球心、半径有关**，与测点 $i$ 无关，记为：

$$
b \;=\; R^2 - C_x^2 - C_y^2 - C_z^2.
$$

于是每个点 $i$ 的约束变成**对 $(C_x,C_y,C_z,b)$ 的一次方程**：

$$
2C_x x_i + 2C_y y_i + 2C_z z_i + b
=
x_i^2+y_i^2+z_i^2.
$$

---

**第 5 步：代数最小二乘在做什么（脚本 `fit_sphere_algebraic`）**

未知数是 $(C_x,C_y,C_z,b)$ 共 4 个。令第 $i$ 个点的**线性残差**为

$$
\varepsilon_i
=
2C_x x_i + 2C_y y_i + 2C_z z_i + b - (x_i^2+y_i^2+z_i^2).
$$

代数拟合求的是

$$
\min_{C_x,C_y,C_z,b}\ \sum_{i=1}^{N} \varepsilon_i^2
=
\min_{C_x,C_y,C_z,b}\ \sum_{i=1}^{N}
\Bigl(
2C_x x_i + 2C_y y_i + 2C_z z_i + b - (x_i^2+y_i^2+z_i^2)
\Bigr)^2.
$$

用 `numpy.linalg.lstsq` 解出 $C_x,C_y,C_z,b$ 以后：

---

**第 6 步：由 $b$ 回到半径 $R$（逐项写回）**

由 $b=R^2-C_x^2-C_y^2-C_z^2$ 移项得

$$
R^2 = b + C_x^2 + C_y^2 + C_z^2,
$$

因此

$$
R = \sqrt{\,b + C_x^2 + C_y^2 + C_z^2\,}
\quad\text{（脚本里要求 }R^2>0\text{）}.
$$

---

**重要**：上面整条「线性化 + 最小二乘」**不是**下面这个几何距离最小二乘问题：

$$
\min_{\mathbf{C},R}\sum_{i=1}^{N}\bigl(\|\mathbf{P}_i-\mathbf{C}\|-R\bigr)^2,
$$

二者一般**不等价**。

---

### 2.2 B 类：无加权几何距离 — `geometric-ls` / `geometric-lm` / `gauss-newton-geometric`

**共同目标（几何最小二乘，写出式子）**：

$$
\min_{\mathbf{C},R}\ \sum_{i=1}^{N}
\Bigl(
\sqrt{(X_i-C_x)^2+(Y_i-C_y)^2+(Z_i-C_z)^2} - R
\Bigr)^2.
$$

记 $d_i(\mathbf{C})=\|\mathbf{P}_i-\mathbf{C}\|$，单点残差 $e_i(\mathbf{C},R)=d_i(\mathbf{C})-R$。

**各方法差异（仅数值路径）**

| `--method` | 怎么求上面的最小值 |
|------------|-------------------|
| `geometric-ls` | `scipy.optimize.least_squares`，`method="trf"`，$R$ 有下界。 |
| `geometric-lm` | 同上目标；参数取 $(C_x,C_y,C_z,\ell)$，$R=\mathrm{e}^{\ell}$；`method="lm"` + 解析雅可比。 |
| `gauss-newton-geometric` | 手写 Gauss–Newton：对 $e_i$ 求雅可比，解正规方程并线搜索；可对 $\mathbf{J}^{\mathsf{T}}\mathbf{J}$ 加小阻尼。 |

---

### 2.3 C 类：协方差加权（法向标量）— 四个 `weighted-*`

**法向量**（从球心指向第 $i$ 个点，单位向量）：

$$
\mathbf{n}_i(\mathbf{C})
=
\frac{\mathbf{P}_i-\mathbf{C}}{\|\mathbf{P}_i-\mathbf{C}\|}
=
\begin{bmatrix} n_{ix} \\ n_{iy} \\ n_{iz} \end{bmatrix},
\qquad \|\mathbf{n}_i\|=1.
$$

**把 $\boldsymbol{\Sigma}_i$ 投影到法向**（标量，表示“沿法向看过去”的方差尺度）：

$$
\sigma_{e,i}^2(\mathbf{C})
=
\mathbf{n}_i(\mathbf{C})^{\mathsf{T}}\,\boldsymbol{\Sigma}_i\,\mathbf{n}_i(\mathbf{C}).
$$

**权重**（脚本里用 $1/\sigma_{e,i}$ 配到残差上）：

$$
w_i(\mathbf{C})=\frac{1}{\sqrt{\sigma_{e,i}^2(\mathbf{C})}}
\quad\text{（代码中对过小值有下限截断）}.
$$

**加权目标（写出式子）**：

$$
\min_{\mathbf{C},R}\ \sum_{i=1}^{N}
w_i(\mathbf{C})^2\,\Bigl(\|\mathbf{P}_i-\mathbf{C}\|-R\Bigr)^2.
$$

因为 $w_i$ 依赖 $\mathbf{C}$，这是**隐式加权非线性最小二乘**。

| `--method` | 说明 |
|------------|------|
| `weighted-geometric-ls` | 上式 + `least_squares` / TRF。 |
| `weighted-geometric-lm` | 同一目标；$R=\mathrm{e}^{\ell}$；LM；`jac="2-point"`。 |
| `weighted-marquardt-geometric` | 同一目标；手写 Marquardt 阻尼步（公式见下）。 |
| `weighted-sequential-ekf` | 与上同一一阶批量加权 GN 目标；每轮解法方程（$h_i$、$R_i$ 见下）。 |

**Marquardt 一步（`weighted-marquardt-geometric`）**：在**当前** $\mathbf{C}$ 处算 $w_i$ 与加权残差 $\mathbf{r}$、雅可比 $\mathbf{J}$ 后，解

$$
\bigl(\mathbf{J}^{\mathsf{T}}\mathbf{J}+\lambda\,\mathrm{diag}(\mathbf{J}^{\mathsf{T}}\mathbf{J})\bigr)\boldsymbol{\delta}=-\mathbf{J}^{\mathsf{T}}\mathbf{r},
$$

调节 $\lambda$ 使加权平方和下降。实现中**不显式**把 $\partial w_i/\partial\mathbf{C}$ 写入 $\mathbf{J}$（常用近似）。

**迭代法方程（`weighted-sequential-ekf`）**：标量伪观测与观测方差

$$
h_i=\|\mathbf{P}_i-\mathbf{C}\|-R,
\qquad
R_i=\mathbf{n}_i^{\mathsf{T}}\boldsymbol{\Sigma}_i\mathbf{n}_i.
$$

每轮对所有点累积 $\mathbf{H}_i=\partial h_i/\partial(C_x,C_y,C_z,R)$ 后组装法方程并解线性方程更新 $(\mathbf{C},R)$；**不是**逐点 Joseph 序贯滤波。

---

### 2.4 D 类：`mahal-orthogonal-ls`（每点 3 个白化残差）

在球面 $\|\mathbf{S}-\mathbf{C}\|=R$ 上取近似垂足 $\mathbf{S}_i$，使白化向量

$$
\mathbf{r}_i = \mathbf{L}_i^{-1}(\mathbf{P}_i-\mathbf{S}_i),
$$

其中 $\boldsymbol{\Sigma}_i=\mathbf{L}_i\mathbf{L}_i^{\mathsf{T}}$（Cholesky）。**目标**：

$$
\min_{\mathbf{C},R}\ \sum_{i=1}^{N} \|\mathbf{r}_i\|^2
=
\min_{\mathbf{C},R}\ \sum_{i=1}^{N}
\bigl(r_{i1}^2+r_{i2}^2+r_{i3}^2\bigr).
$$

即每个点贡献 **3 个标量残差**（比 C 类多利用协方差在切向上的结构，垂足仍为近似）。

---

### 2.5 E 类：`directional-ls`（射线 + 斜距）

射线 $\mathbf{q}(t)=t\mathbf{u}_i$，$t>0$。与球 $\|\mathbf{q}-\mathbf{C}\|=R$ 联立得关于 $t$ 的二次方程（脚本等价形式）：

$$
t^2 - 2(\mathbf{u}_i^{\mathsf{T}}\mathbf{C})\,t + \bigl(\|\mathbf{C}\|^2-R^2\bigr)=0.
$$

判别式 $\Delta\ge 0$ 时取两个非负根中**更接近观测斜距 $d_i$** 的那一个，记为 $t_i(\mathbf{C},R)$。

**默认（`ray-mahalanobis`）目标**：

$$
\min_{\mathbf{C},R}\ \sum_{i=1}^{N}
(d_i-t_i(\mathbf{C},R))^2
\Bigl(
\mathbf{u}_i^{\mathsf{T}}\boldsymbol{\Sigma}_i^{-1}\mathbf{u}_i
\Bigr).
$$

**可选（`distance-only`）**：

$$
\min_{\mathbf{C},R}\ \sum_{i=1}^{N}
\frac{(d_i-t_i(\mathbf{C},R))^2}{\sigma_d^2}.
$$

---

### 2.6 快速对照（选方法时只看这里）

| 需求 | 建议看的节 |
|------|------------|
| 快速初值 | **2.1** |
| 不用 $\boldsymbol{\Sigma}_i$ | **2.2** |
| 用全站仪传播 $\boldsymbol{\Sigma}_i$ 且法向标量加权 | **2.3** |
| 每点 3 个马氏分量残差 | **2.4** |
| 在 $(\mathbf{u}_i,d_i)$ 域直接拟合 | **2.5** |

**关于“几何对噪声敏感”**：**2.1 代数**通常更怕粗差；**2.2 几何**模型本身清楚，问题多在数据/系统模型与收敛，而不是“几何”二字本身。

---

## 3、结论

1. 误差应分：随机（经 $\mathbf{J}_i$ 到 $\boldsymbol{\Sigma}_i$）、构形、系统模型、粗差、数值误差。  
2. **加权**把 $\boldsymbol{\Sigma}_i$ 带进目标函数，**不**代替非球/偏心等系统项建模。  
3. **同类求解器不同**时 $(\mathbf{C},R)$ 应接近；RMS 若仍高，优先查数据与模型，而不是再堆优化器名字。

与实现完全一致时，请以当前版本 `sphere_fitting.py` 及命令行参数为准。
