# 不规则平面图形几何质心：选定的高精度流程

本文约定：目标为**均匀密度平面薄片**的**几何质心**（形心），输入为单通道或经处理后的**前景二值分割**或**可稳定提取的轮廓**。在分割正确的前提下，离散栅格上的主要误差来自**轮廓落在像素网格上的阶梯近似**；因此将**轮廓定位到亚像素**后，用**闭合多边形的精确几何质心公式**计算，是在该类问题中**可复现、可验证的精度优先方案**（优于仅用整像素掩膜做 `m10/m00`、`m01/m00`，除非掩膜本身已是亚像素栅格化结果）。

---

## 一、选定方法（唯一结论）

| 项目 | 内容 |
|------|------|
| **方法名称** | **亚像素轮廓细化 + 多边形 Shoelace / Green 几何质心（双精度）** |
| **核心思想** | 几何质心由区域一阶矩定义；将区域边界表示为**亚像素闭合多边形**，再用**解析公式**求质心，避免整像素边界引入的系统性阶梯误差。 |
| **轮廓亚像素细化（实现层）** | 使用 OpenCV **`cornerSubPix`**，在灰度图上沿法向利用梯度迭代，将 `findContours` 得到的轮廓顶点坐标细化到亚像素（与标定、目标检测中常用的亚像素角点/边点细化一致）。 |

**说明（避免过度承诺）**：若输入为**灰度光斑 / 点扩散函数**且噪声以**光子散粒噪声**为主，统计意义上的“最优质心”需另选**噪声模型下的无偏估计**（例如与 Cramér–Rao 界对照的文献方法），与本节**几何质心**不是同一问题。本文仅锁定**不规则二值区域的几何质心**。

---

## 二、数学定义（与实现对应）

对简单闭合多边形顶点 \((x_i, y_i)\)，\(i=0,\ldots,n-1\)，令 \(x_n=x_0,\,y_n=y_0\)。

**有向面积（Shoelace）**：

\[
A = \frac{1}{2}\sum_{i=0}^{n-1}(x_i y_{i+1} - x_{i+1} y_i)
\]

**质心**（\(A\neq 0\) 时）：

\[
C_x = \frac{1}{6A}\sum_{i=0}^{n-1}(x_i + x_{i+1})(x_i y_{i+1} - x_{i+1} y_i),\quad
C_y = \frac{1}{6A}\sum_{i=0}^{n-1}(y_i + y_{i+1})(x_i y_{i+1} - x_{i+1} y_i)
\]

实现时使用 **64 位浮点**（如 NumPy `float64`）累加；顶点顺序须**一致**（全逆时针或全顺时针），\(A\) 的符号会相反，\(C_x,C_y\) 不变。

---

## 三、处理流程（按顺序执行）

### 步骤 1：图像与分割假设

- 输入：灰度图 `I`（`uint8` 或更大动态范围）或已由上游给出的**二值掩膜** `M`。
- 若尚未二值化：根据场景选用**固定阈值 / Otsu / 自适应阈值**等，得到前景掩膜 `M`（必要时形态学开闭运算去噪、填洞）。
- **输出**：二值掩膜 `M`，前景为 255（或 1），背景为 0。

### 步骤 2：提取最大连通前景与轮廓

- 对 `M` 做连通域分析，保留**面积最大**的前景连通域（或按业务规则选择目标）。
- `findContours` 提取外轮廓（建议 `CHAIN_APPROX_NONE` 保留完整边界链码，便于后续均匀重采样；若点数过多可先 `CHAIN_APPROX_SIMPLE` 再重采样）。
- **输出**：单条闭合轮廓点序列（像素级浮点坐标可先存为 `float32`，细化后再转 `float64` 计算质心）。

### 步骤 3：轮廓点重采样（建议）

- `cornerSubPix` 对**角点状**结构最稳；对**长直边**，轮廓链上相邻点过密或过稀都会影响迭代初值。
- **建议**：按弧长或索引对轮廓**均匀重采样**为固定步长（例如每 2～4 个像素一个点），得到 `N` 个顶点；再送入亚像素细化。
- **输出**：重采样后的顶点列表 `P = {(u_k, v_k)}\}`（图像坐标，原点左上，\(u\) 为列、\(v\) 为行）。

### 步骤 4：亚像素轮廓细化（本方案的核心）

- 在灰度图 `I` 上调用 **`cornerSubPix`**：
  - 初值：重采样轮廓点（`float32` 的 \((u,v)\)）。
  - **窗口** `winSize`：奇数，典型 `5×5` 或 `7×7`，随纹理与噪声调整。
  - **零区域** `zeroZone`：常用 `(-1,-1)`。
  - **终止准则** `criteria`：例如 `EPS + MAX_ITER`，`max_iter` 30～100，`epsilon` 0.001～0.01（像素）。
- **输出**：亚像素顶点 `P' = {(u'_k, v'_k)}\}`。

**公开文档（可核对 API）**：[OpenCV 交互式亚像素角点检测示例](https://docs.opencv.org/4.x/dc/dbb/tutorial_py_calibration.html)（文档中包含 `cornerSubPix` 用法与参数说明）。

### 步骤 5：闭合多边形与质心计算

- 将 `P'` 视为闭合多边形（首尾与 `findContours` 约定一致；若 OpenCV 未自动闭合，需在公式中显式闭合）。
- 用第二节 **Shoelace / Green** 公式在 **`float64`** 下计算 \(C_x, C_y\)。
- **输出**：\((C_x, C_y)\)，单位为**像素**，亚像素精度；坐标系与 OpenCV 一致（原点图像左上角，\(x\) 向右、\(y\) 向下）。

### 步骤 6：质量检查与不确定度（强烈建议）

- **面积符号**：检查 \(A\) 是否与预期方向一致；\(|A|\) 应与物理面积量级相符。
- **重复性**：固定场景连拍 \(K\) 帧，对每帧独立分割+全流程，报告 \(\mathrm{std}(C_x),\mathrm{std}(C_y)\) 或欧氏半径。
- **合成验证**：在空白图上绘制已知质心的多边形或填充形状，用同一流程反算误差（像素 RMSE）。

---

## 四、可选对照（同一掩膜上的等价校验）

将亚像素多边形**栅格化**为高精度掩膜（高分辨率画布缩放绘制再缩放回原分辨率，或直接对多边形内点判断），对掩膜用 **空间矩** 计算：

\[
\bar{x} = \frac{m_{10}}{m_{00}},\quad \bar{y} = \frac{m_{01}}{m_{00}}
\]

OpenCV：`cv.moments`（保持浮点，勿强制转整型坐标）。  
**公开文档**：[轮廓矩与质心教程](https://docs.opencv.org/4.x/dd/d49/tutorial_py_contour_features.html)、[Moments 类](https://docs.opencv.org/4.x/d8/d23/classcv_1_1Moments.html)。

在数值上，细化多边形质心与高质量栅格化后的矩质心应**一致到小数多位**；若差异大，优先检查轮廓方向、闭合性与重采样。

---

## 五、与“更底层亚像素边缘”文献的关系（扩展阅读）

若边界呈**阶跃边缘**且需从灰度直接估计边缘参数，经典路线为 **Zernike 正交矩算子**（亚像素边缘）。这与本流程中 **cornerSubPix（基于局部灰度梯度迭代）** 属于不同实现路径；工程上常二选一或组合：**粗轮廓 + cornerSubPix** 往往已足够，且依赖少。

可核对书目的公开条目：  
S. Ghosal, R. Mehrotra, *Pattern Recognition*, 26(2), 295–306 (1993)，  
https://www.sciencedirect.com/science/article/abs/pii/003132039390038X  

---

## 六、小结表

| 步骤 | 操作 | 产出 |
|------|------|------|
| 1 | 预处理 + 二值分割 | 掩膜 `M` |
| 2 | 最大连通域 + `findContours` | 像素轮廓 |
| 3 | 均匀重采样 | 顶点集 `P` |
| 4 | `cornerSubPix` 于灰度图 `I` | 亚像素顶点 `P'` |
| 5 | Shoelace 质心（`float64`） | \((C_x,C_y)\) |
| 6 | 重复性 / 合成真值检验 | 不确定度报告 |

---

*文档版本：与仓库内标定/图像处理脚本并列存放，便于与 `calibrate.py` 等流程统一维护。*
